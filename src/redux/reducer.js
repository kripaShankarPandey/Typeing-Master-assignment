const initialState={
    keyCount:0,
    str:["a","s","d","f","j","k","l",";"]

}
export const reducer=(state=initialState,{type,payload})=>{
    switch(type){
        default:{
            return initialState
        }
    }
}